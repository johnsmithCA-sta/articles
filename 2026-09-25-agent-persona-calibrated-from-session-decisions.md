\---  
title: Agent 的人格不是写出来的，是被纠正出来的  
description: 一个常驻 Agent 四轮记忆治理的实录。核心观点：人格与用户画像不该是先验宣言，也不该从对话统计里优化出来，而应该从会话任务的具体决策中倒推校准。  
author: GitHub ID：johnsmithCA-sta ｜ 掘金：johnsmithCA  
date: 2026-09-25  
last_updated: 2026-09-25  
version: 1.0.0  
status: published
tags: [AI Agent, LLM, 长期记忆, SOUL.md, 个性化, 记忆治理]  
license: CC BY-SA 4.0  
\---

# Agent 的人格不应该先写出来，应该被会话决策纠正出来

最近高频使用Agent平台，跨好几条项目线在干活，日调用token巨大。Agent记忆被我在实操过程中分成了七层，大家都知道其中最特殊的三层——人格（SOUL.md）、用户画像（USER.md）、身份（IDENTITY.md）——会在每个会话启动时**逐字注入** system prompt。有一天我发现，用户级记忆文件已经长到 8,912 字节，而注入配额只有 4,000。超出的部分被**静默截断**：不报错、不告警，文件本身完好无损。

也就是说，这个 Agent 的记忆缺了后三分之一，它自己对此毫无察觉，每天还以「我全记得」的姿态自信作答。

这件事让我意识到一个反直觉的事实：人格层的完整性问题不在存储层，而在**读取路径**上，而且受损方恰恰是最没能力自检的那一方。后来围绕这套记忆体系，我前后做了四轮治理，踩了九个坑。踩完之后的结论，和当前主流的两条人格构建路线都不一样：

**Agent 的人格与用户画像，既不是人能预先写对的（有很多文章都在教怎么写soul），也不该从对话统计里优化出来——应该从会话任务的具体决策里倒推出来。**

这篇就讲这个判断是怎么来的，聊聊观点和思路。

## 背景：这类文件现在主流长什么样

先把概念对齐。现在给 Agent 配「持久自我」的做法已经很普遍了：

- Claude Code 读 `CLAUDE.md`，Cursor 读 `.cursor/rules`，Windsurf 读 `.windsurfrules`，GitHub Copilot 读 `.github/copilot-instructions.md`，跨厂商还有个 `AGENTS.md` 公约；
- OpenClaw在工作区里拆出了一组文件：`SOUL.md` 定人格与价值观，`USER.md` 建用户画像，`IDENTITY.md` 记身份，`MEMORY.md` 存长期记忆，`memory/YYYY-MM-DD.md` 记每日流水；
- 还有人在尝试把它标准化，比如 SoulSpec，用一个 `soul.json` 清单加一组 markdown 文件，让人格包可以跨框架迁移。今年 MSR 2026 上有篇研究（arXiv:2510.21413）扫了 466 个开源 Agent 项目，结论是大家都在写 persona 文件，但没有任何公认的结构。

这些文件里，人格与用户画像这层是最特殊的，原因就在注入方式：别的记忆是按需检索的，它是**每个会话逐字进 system prompt** 的。这带来两个工程性质：每一个字都持续占用上下文预算；每一处错误都会放大到所有任务。

所以需要思考的问题就来了：**这层文件的内容，到底该从哪来？**

## 两条路线，各有各的硬伤

### 路线一：人格是定义出来的

先由人（或 Agent 自己）写好人设和原则，之后按需修订。OpenClaw 生态的 SOUL.md 模式就是这么设计的，而且有个关键机制叫 **mutability**：Agent 可以编辑自己的 soul 文件，遇到新情况、做了决策，就把它写回身份定义。学术界也在把这件事形式化，比如 arXiv:2601.10025 那篇 Structured Personality Control，里面三个机制之一的 Reflection——定期回顾近期行为、对照规格、更新 self-model——基本就是 SOUL.md 自编辑的学术版。

个人认为这条路线的硬伤是**证据锚点缺失**。Agent 既是行为的发生者，又是行为记录的修订者，还是人格更新的执行者，三重身份合一。它写进人格的，是它对自己「为什么这么做」的事后解释，不是经过验证的行为规律。说白了，就是让考生自己批改自己的卷子，卷子还被它润色过。模型对自身行为的归因偏差，会原封不动沉淀成持久人格。

### 路线二：人格是从交互里长出来的

也有一些更加激进的研究。PersonaAgent（arXiv:2506.06254，ACL 2025 Findings）把 persona 定义成连接记忆与行动的中间层，并给出 test-time 对齐：模拟最近若干轮交互，用模拟响应和真实用户响应之间的文本损失，迭代重写 persona prompt。AI PERSONA（arXiv:2412.13103）把用户画像建成可学习的字典，由一个 LLM 充当的 persona optimizer 在每轮会话后更新字段。DEEPER（arXiv:2502.11078，ACL 2025）则用「模型预测 vs 用户实际行为」的误差当奖励，两阶段 DPO 训练，把用户行为预测误差平均降了 32.2%。

这条路线优化目标明确，但落到真实助手场景会卡在一个地方：**需要稠密的真值信号**。PersonaAgent 要用户的 ground-truth 响应，DEEPER 的场景是推荐系统——那里有评分、有点击，反馈天然稠密。而真实会话里，用户不会给 Agent 的每一步打分。反馈稀疏、噪声大、时点滞后。而且这条线的信号源是「交互的统计特征」，不是「任务的决策内容」——这是两个东西，后面会说区别有多大。

### 两条线之间还隔着一堵墙

记忆侧的主流工作——MemGPT、Mem0、A-MEM，以及 Generative Agents 的 memory stream + reflection 范式——和人格侧基本不交叉：前者的 reflection 产物是事实条目和经验总结，**不回写人格层**；后者更新 persona 时，也**不消费结构化的任务决策记录**。「记住了什么」和「我是谁、用户是谁」，在现有体系里是两条互不供养的管线。

所以分歧的本质就一句话：**人格更新的信号源选什么，以及凭什么可信。** 宣言可以自洽但会失真；优化目标明确但真值难取。

## 我的观点：倒推是对的，但证据源要收窄到「任务决策」

我的立场在路线二 这一边，一方面，我坚定的认为，大模型或者说agent的进化，一定是由A I自己来完成，这是大趋势。另一方面，在具体的操作上，我认为要把证据源砍窄：**不是交互历史，而是会话任务中的具体决策。**

对此，我专门研究了人的记忆形成等认认知科学，结合个人对「我为什么是我」等哲学命题的思考。借用自认知科学中自我知觉理论，人并不是先确立「我是谁」再行动，而是通过观察自己的行为倒推自我概念——人格是行为证据的收敛结果。对 Agent 来说，对应的「行为」不是对话措辞，而是任务现场的三类关键事件：

- **纠正**：同一个偏好在多个会话里反复纠正。我设置的固化阈值是 2~3 次——少于这个次数的「偏好」很可能是临时的。有真实教训：曾把某个只维持了一周的模型路由策略固化进记忆，后来花了额外一轮治理才清掉；
- **拍板**：用户对方案选型的否决或确认，尤其是推翻 Agent 自作主张的那些时刻，是用户主体性绝对意志的体现，对于「养Agent 」这件事不容置疑的重要；
- **口径**：以硬约束形式下达的规则（比如「脚本默认不覆盖人工校改」），以及这些约束后来的撤销。

为什么把闲聊、情绪表达、单次指令排除在证据源之外？因为自述偏好（「我喜欢简洁的回复」）和决策行为（连续三轮否掉长回复方案）之间，后者的可靠性高得多。人会说一套做一套，Agent 面对的用户也一样。

于是人格文件里的每一行原则，都得能回答一个溯源问题：**你从哪个任务决策里知道这一点的？** 答不上来的条目，降级为待验证假设，不以「原则」的姿态待在注入层。

我认为这个立场把两条路线各救了一半：保留路线二 的后验性（人格是收敛结果，不是先验设定），又绕开路线 二 的真值难题——任务决策本身就是带语义标签的真值信号，不需要用户逐句打分；同时给路线一 的自编辑装上外部锚点——Agent 可以提议修订，但每条修订必须挂证据，经确认才生效。

## 主流的记忆管理流程，为什么接不住这个目标

社区里关于 Agent 记忆的实践经验是有共识的：分层——日志管流水账、MEMORY.md 管长期记忆、定期蒸馏加回顾。这套共识没错，但只回答了「怎么存」。拿「人格倒推校准」当目标去反推，会发现三个结构性缺口：

1. **入口没闸。**「克制地记」是倡导，不是机制。Agent 在会话里写一条记忆的边际成本是零，而污染的代价要在好几个会话之后才显现。开头那个 8,912 字节就是这么长出来的。
2. **出口没门。**&#x8BA8;论都集中在写入和压缩，蒸馏后的原始素材去哪了、待办怎么核销、失效条目怎么降级，鲜有方案。所有只进不出的系统，积压是必然的，曾经有一周，工作量一大、日志一多，我设置的memory限额反复超限。
3. **人格没证据链。**「定期让 Agent 自查」是主流答案，但自查既没有证据锚（路线 A 的老问题），也没有对账机制——宣言和行为是否一致，没有可判定的指标。

## 治理数据实录：列点数据

| 轮次  | 触发                  | 关键动作           | 结果                             |
| --- | ------------------- | -------------- | ------------------------------ |
| R1  | 注入被截断               | 压回配额           | 用户级 8,912B → 4,000 内；确认截断是静默丢失 |
| R2  | 再度超限                | 逐块归属分析         | 48% 内容放错层；一字未删，只挪位置就降了 58%     |
| R3  | 项目级memory涨到配额的 249% | 外置 + 红线 + 核对机制 | 降 60%；但 24 小时内回弹 249%，逼出了写入前预检 |

四轮之后沉淀下来的机制，全部围绕「决策证据 → 人格」这条管线：

**记忆写入要在落盘前拦。** 写入前先做预检：预计超过硬配额红线就拒绝，必须先置换再写入。事后检查没有意义——污染已经落盘了。配合几道判据：这东西几天后还有用吗？别的层是不是已经有了（有了只放指针）？是事实还是未验证的判断？涉不涉及凭据（只留指针）？给出「没问题」结论的工具自证过没有？

**记忆蒸馏时，证据要当一等公民留。** 日志蒸馏的时候，「人格/画像反推信号」——那些「用户又纠正了我一次」「这个方案被否了」的记录——要和待办、结论**同级保留**，明确标记，禁止当噪音压掉。这是我最看中的一个设计：大多数蒸馏流程恰恰会把这些「看起来像流水账的纠正记录」压缩掉，而它们是人格校准唯一的原料。

**校准要做成对账，不是感想。** 周期性体检里，校准画像和校准人格这两步，输入就是上面攒下的决策证据。校准动作带对账语义：人格里写着「结论必须先给数字」，而近 30 天的决策记录里找不到一次相关佐证——这条要么补证据，要么降级。人格漂移因此从「感觉最近不太对」变成可判定的指标：**宣言与证据的覆盖差**。

## 还没想明白的两件事

如实列出，不装闭环：

1. **多实例记忆合并没有方案。**&#x5E76;发防护只解决了单机多会话的竞争。多台机器各自积累的记忆怎么 diff、冲突怎么仲裁、人格证据怎么去重合并，空白。
2. **校准自动化的边界在哪。**&#x76EE;前人格修订要人工确认才生效。完全自动化意味着 Agent 改写自己的人格——闭环理论上成立，但路线 A 的自我证成问题和「PASS 工具」那个坑指向同一个风险：**校准者不能是被校准者**。自动化到哪一级停手，我没想清楚，欢迎讨论。

## 最后

需要坦白的一点：从养龙虾走到现在，Agent的记忆机制还在不断发展过程中，也许以后这都会内化为成为LLM的本能，智能时代，也许这以后都是不该操心的事。个人实践是一方面，但外部有效性我不敢打包票。这也是为什么我认为「人格证据源的界定」和「校准自动化的边界」这两个问题，比记忆分层本身更值得投入。
